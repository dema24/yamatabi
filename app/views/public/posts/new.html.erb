<div class="container">
  <h2>新規投稿</h2>
  <div class="row">
    <div class="offset-md-2 col-md-9 px-5 px-sm-0 mt-3 text-nowrap">

      <%= form_with model: @post do |f| %>
      <%= render 'layouts/errors', obj: @post %>
      <table class="table table-borderless">
        <tr>
          <th><%= f.label :image, "投稿画像" %></th>
          <td><%= f.file_field :images, multiple: true, class: "form-control-file user_image", accept: 'image/*' %></td>
        </tr>
        <tr>
          <th><%= f.label :title, "タイトル" %></th>
          <td><%= f.text_field :title, autofocus: true, required: true, class: 'form-control' %></td>
        </tr>

        <tr>
          <th><%= f.label :body, "投稿文" %></th>
          <td><%= f.text_area :body, autofocus: true, class: 'form-control' %></td>
        </tr>

        <tr>
          <td></td>
          <td><%= f.submit "変更内容を保存する" , class: 'btn btn-success form-control'%></td>
        </tr>
      <% end %>
      </table>
