<% if admin_signed_in? %>
  <%= form_with url: admin_search_path, method: :get do |f| %>
    <%= f.text_field :content, required: true %>
    <%= f.select :model, options_for_select({"会員" => "user", "投稿" => "post"}) %>
    <%= f.submit '検索' %>
  <% end %>
<% end %>